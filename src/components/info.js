import React, { Component } from 'react';

const Info = props => (
  <div className="info">
    <h1>Weather Application</h1>
    <p>Find out the weather in your city</p>
  </div>
);

export default Info;